# OpenMemory 快速开始指南 ⚡

> 5分钟内完成 OpenMemory 的安装和配置

## 📋 前置要求

- ✅ Docker Desktop (Windows/Mac) 或 Docker Engine (Linux)
- ✅ OpenAI API 密钥 (或兼容的 API 服务)
- ✅ 2GB+ 可用内存

## 🚀 三步启动

### 步骤 1: 下载项目

```bash
git clone <repository-url>
cd openmemory
```

### 步骤 2: 配置 API 密钥

```bash
# 复制配置文件
cp api/.env.example api/.env
cp ui/.env.example ui/.env

# 编辑 api/.env，只需设置这一项:
# OPENAI_API_KEY=sk-your-api-key-here
```

**Windows 用户**:
```powershell
notepad api\.env
```

**Mac/Linux 用户**:
```bash
nano api/.env
# 或
vim api/.env
```

### 步骤 3: 启动服务

```bash
docker compose up -d
```

等待 30-60 秒，服务启动完成！

## ✅ 验证安装

打开浏览器访问:

- **前端界面**: http://localhost:4000
- **API 文档**: http://localhost:8765/docs

如果能看到界面，说明安装成功！🎉

## 🔧 配置 MCP 客户端

### Claude Desktop

1. 访问 http://localhost:4000
2. 找到 "Install OpenMemory" 区域
3. 点击 "Claude" 标签
4. 复制显示的命令
5. 在终端运行:

```bash
npx install-mcp i http://localhost:8765/mcp/claude/sse/user --client claude
```

### Cursor / Cline / Windsurf

同样的步骤，选择对应的标签页，复制并运行命令即可。

## 💬 开始使用

配置完成后，在你的 AI 客户端中:

```
你: "记住我喜欢喝咖啡"
AI: [自动保存到 OpenMemory]

你: "我喜欢喝什么?"
AI: [从 OpenMemory 检索] "根据我的记忆，你喜欢喝咖啡"
```

## 🛠️ 常用命令

```bash
# 查看服务状态
docker compose ps

# 查看日志
docker compose logs -f

# 停止服务
docker compose down

# 重启服务
docker compose restart

# 完全清理 (包括数据)
docker compose down -v
```

## 🌐 使用国内 API 服务

如果你使用硅基流动、DeepSeek 等国内服务:

```env
# api/.env
OPENAI_API_KEY=sk-your-siliconflow-key
OPENAI_PROVIDER=openai_structured
OPENAI_BASE_URL=https://api.siliconflow.cn/v1
OPENAI_MODEL=deepseek-ai/DeepSeek-R1

OPENAI_EMBEDDING_MODEL_BASE_URL=https://api.siliconflow.cn/v1
OPENAI_EMBEDDING_MODEL=BAAI/bge-large-zh-v1.5
OPENAI_EMBEDDING_MODEL_DIMS=1024
```

## ❓ 遇到问题?

### 端口被占用

```bash
# Windows
netstat -ano | findstr "8765"
netstat -ano | findstr "4000"

# Mac/Linux
lsof -i :8765
lsof -i :4000
```

### 服务启动失败

```bash
# 查看详细日志
docker compose logs -f openmemory-api

# 重新构建
docker compose down
docker compose build --no-cache
docker compose up -d
```

### API 密钥错误

确保 `api/.env` 中的 `OPENAI_API_KEY` 正确设置，没有多余的空格或引号。

## 📚 下一步

- 📖 阅读 [详细使用指南](README_详细使用指南.md)
- 🔍 浏览 [API 文档](http://localhost:8765/docs)
- 💻 查看 [配置优化建议](配置优化建议.md)

## 🎯 核心功能

- ✅ **自动记忆**: AI 自动保存重要信息
- ✅ **智能搜索**: 语义搜索，理解你的意图
- ✅ **多用户**: 支持多个用户独立使用
- ✅ **分类管理**: 自动为记忆分类
- ✅ **Web 管理**: 可视化管理所有记忆

---

**需要帮助?** 查看 [常见问题](README_详细使用指南.md#常见问题) 或提交 Issue